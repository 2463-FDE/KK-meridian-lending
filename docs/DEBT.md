# Debt register

The `D`- and `RF`- numbers cited throughout this repository's code comments,
ADRs and runbook. They were being cited before they were written down anywhere,
so a reader hitting "(debt D7)" in a source file had no way to find out what D7
was. This file is that lookup.

**Numbering.** `D<n>` are defects inherited from the Halcyon handoff or found
while reading the code. `RF-<n>` are review findings raised during this
engagement. Neither sequence is contiguous — gaps are numbers that were assigned
during review and later folded into another entry, and renumbering them would
break the citations already committed in code.

**Status** uses the labels from [ARCHITECTURE.md § Status
legend](../ARCHITECTURE.md#status-legend). "Fixed" means there is a test that
fails without the fix, not that it looked right on inspection.

An entry that is **not** fixed says who it is waiting on, using the same labels
as the Week 7/8 status blocks in [docs/ROADMAP.md](ROADMAP.md):
**CLIENT-BLOCKED** (a policy or appetite decision), **CLIENT-DEFERRED** (the
client has the question and has not answered it), **VENDOR-BLOCKED** (a taxonomy
or document nobody has supplied), **OPS-BLOCKED** (a deployment decision -- where
a page goes, who is on call, what escalates), **CLIENT-PROHIBITED** (the client
has answered and the answer is *no*, for now). "Open" on its own reads as unbuilt
code, and for several of these entries the code is not the missing part.

**CLIENT-PROHIBITED is not CLIENT-BLOCKED or CLIENT-DEFERRED, and the difference
decides what a reader may do next.** Blocked and deferred both mean a decision is
outstanding, so the honest next step is to go and ask. Prohibited means the
decision was made and it forbids the thing: asking again is not the next step,
and building it "ready to switch on" is the defect rather than preparation. It
arrived with the client's decision of 2026-08-24, which prohibited every external
alert channel and any new credential before the freeze -- an item can therefore
be OPS-BLOCKED and CLIENT-PROHIBITED at once (D7 is), and both have to lift.

Every entry below was written from the citation site in the code, not from
memory. The "Cited at" column is where to look for the current detail.

## Defects

| ID | What | Status | Cited at |
|---|---|---|---|
| **D1** | Money math done in float rather than Decimal on the disclosure read path. The offer/APR/schedule write paths computed in Decimal, but the read path rebuilt the display schedule in float, so a redisplayed schedule could disagree by a cent with the disclosure printed above it. | **Fixed.** `disclosure-service/app/routers/offers.py` no longer casts on the way in (`_dec` recovers the exact cent value; the raw-cursor path reads `NUMERIC` as `Decimal` and never calls `float()` on it), the schedule expansion and the half-cent residue check run in Decimal, and the only remaining `float()` calls are the response-model constructions at the serializer boundary. `disclosure-service/tests/test_redisplay_is_exact.py` asserts equality in Decimal rather than within a tolerance — a tolerance is the thing that lets a cent move — and `test_every_amount_stays_decimal_through_the_read_path` fails on a single float anywhere in a row. **Bound of the claim:** the ORM path loads these columns with `asdecimal=False` (`disclosure-service/app/models.py`), so a value transits binary float before `_dec` recovers it. For `NUMERIC(14,2)` money that round-trip is exact, so no cent can move; "Decimal from the database read" is true of the arithmetic, not of the driver hand-off. *This row read "Partly fixed" against `main` for four days after the fix merged.* | `disclosure-service/app/routers/offers.py`, `disclosure-service/tests/test_redisplay_is_exact.py` |
| **D2** | A `POST /payments` with no idempotency key: a retried request recorded the payment twice and applied the balance twice. On the processor-backed path it could also re-authorize the card. **Two endpoints had that name, and only one of them talked to a processor.** | **Fixed.** **The canonical path** — `payment-service`'s `POST /payments` — requires `idempotency_key`, holds it with a partial unique index (`db/migrations/0007`), inserts atomically with `ON CONFLICT DO NOTHING`, applies once through `payment_applications` (`0013`), and does not re-call `authorize_charge()` on a retry. **The legacy duplicate is retired**: `servicing-service`'s processorless `POST /payments`, its `PaymentIn` schema and `app/payments.py` are deleted, so servicing has no payment-creation path at all — a retry cannot double-record or double-apply through it because there is nothing to call. Nothing used it: the gateway matched no rule for `/lss/payments` and 404'd, no frontend referenced it, and payment-service never called it. Deleted rather than disabled — a flagged-off money route keeps its schema, its imports and its place in the next reader's model, and re-enabling it is a one-line mistake. **History preserved:** rows it wrote keep `capture_source='servicing_legacy'` (`db/migrations/0042`), the value stays permitted by the CHECK constraint, and reconciliation still counts and excludes them — they have no processor behind them and never will (see D7). That population is now closed rather than growing. **Evidence:** `servicing-service/tests/test_legacy_payments_route_is_retired.py` (route absent from the routing table, call reaches no money code, module unimportable, no `servicing_legacy` INSERT remains, the label still schema-valid), mutation-verified by restoring the route — 5 of its 7 cases fail. | `payment-service/app/main.py`, `services/gateway/app/main.py` |
| **D3** | `balance.apply_payment` is a read-modify-write with no row lock — two concurrent payments can lose one update. | **Fixed.** The apply path writes an immutable `ledger_entries` row and `balances` is maintained by the projection trigger, which composes signed deltas (`UPDATE balances SET balance = balance + NEW.amount`) instead of writing a total read earlier in the transaction — two concurrent payments now both survive (`db/migrations/0035_ledger_entries.sql`; `servicing-service/app/balance.py::apply_payment_once`). **Evidence, executed:** `servicing-service/tests/test_balance_lost_update_real_postgres.py` (5 cases) and `db/tests/test_0035_ledger_projection.py` (64 cases) were run against **PostgreSQL 16.14** on **2026-08-15** and all 69 passed, alongside the rest of `db/tests` (649 passed, 0 skipped) and servicing (296 passed, 0 skipped). Corroborated independently by CI on this exact commit — GitHub Actions run `31759960436`, head `c91fd19`, 2026-08-14T01:14Z, jobs `db-migrations` and `backend (servicing-service)` green. *Recorded because the first pass could not do this: with the Docker daemon down and `DATABASE_URL` unset, all 69 skipped, and this row cited CI alone while saying so. A skip reads exactly like a pass, so the entry named the limitation rather than implying the tests had run — and then the tests were actually run.* **Residual, not a lost update:** `apply_payment` (the legacy helper) returns a `new_balance` computed from a read taken before its own `UPDATE`, so its *return value* can be stale under concurrency even though the stored balance is right. | `servicing-service/app/balance.py`, `db/migrations/0035_ledger_entries.sql` |
| **D4** | `decisions` stored outcome only: no reason, no score, no inputs, no timestamp, for any application ever. | **Fixed** — append-only `decision_events` (Week 3, ADR 0006), trigger-enforced | `decision-service/app/models.py` |
| **D5** | PII in logs and in storage. Two halves, tracked together because they were reported together. | **Split** — see below | `ARCHITECTURE.md`, `payment-service/app/main.py` |
| D5a | Card/PII written to logs at INFO with no redaction. | **Fixed, bounded to what was verified.** Not a claim that PII logging in general is fixed. What was removed at application level: **raw request bodies, and the high-risk fields SSN, DOB, address, PAN and CVV.** Re-verified 2026-08-07 by reading every `log.*` call site in `services/*/app/` and by executing `redact_dict` on a representative charge payload, not by grepping for a middleware name. Per site: `services/origination-service/app/intake.py` → `app_id`/`applicant_id` (it stopped logging the request payload in the PR #6 review, Gap C); `services/kyc-service/app/routers/kyc.py` → `application_id`/`applicant_id`; `kyc/kyc.py::run_cip` → the four CIP booleans — it **previously logged the applicant's name**, fixed in the same review and on `main` since, so that is historical behaviour and not a current gap; `services/decision-service/app/graph.py` → `app_id`, score, decision, reason codes; `payment-service/payments.py::charge` → `redact_dict` output, in which `processor_token` is replaced with `[REDACTED]` (verified by execution) and the cardholder `name` is no longer passed at all (D5d, fixed in this PR); `servicing/payments.py::charge` → `loan_id`/`amount`/`method` only (it received `processor_token`/`last4`/`brand` under ADR 0008 but passed none of them to the logger). **That module is now deleted** with the processorless route it served (D2), so this call site no longer exists; the entry is kept because the verification it records covered it at the time. **No raw PAN or CVV reaches this path under a field of that name — `PaymentIn`'s `extra="forbid"` matches field names, not content, so the guarantee for card data pushed through an *allowed* field (a PAN in `processor_token`) is `redact_dict`, which masks sensitive keys and applies the PAN/SSN/CVV patterns to every other string value before the log call. Processor tokens and last4 remain security-relevant and must not be unnecessarily logged.** **What is still logged, deliberately:** identifiers and financial decision data — applicant/application/loan IDs, model scores, decisions, reason codes, balances, amounts, and `last4`/`brand`. These are the correlation and audit fields operations needs; they are not claimed to be free of privacy consequence. **No service has request-body middleware**; the gateway's `await request.body()` is read to forward, never logged. *This row previously recorded servicing and origination as still open — that was read off a stale docstring rather than the code (see D5c).* **Scope of this verification:** verified against current application-level logging call sites. Reverse-proxy, container-runtime and deployment-platform logging configurations were not available in this repository and were not independently tested. | `origination-service/tests/test_intake_pii_not_logged.py`, `payment-service/app/payments.py` |
| D5c | Four `logging_config.py` docstrings claimed *"Logs the full request body on every POST — including PII. No redaction. Halcyon said 'we need the body to debug.'"* — about modules that only wire a stream handler and a file handler and log nothing themselves. Copy-pasted across origination, decision, kyc and (charge-specific variant) servicing. | **Fixed** — each docstring now describes only what its module does, and the history lives here instead of in four source files. **Recorded because the cost was real:** this comment is why D5a was reported as a live PII-logging gap twice — once in this register, once in a later documentation pass — both times by a reader trusting a comment over a call site. Neither reader grepped for the middleware the comment implied; it has never existed. **The general rule: a comment that overstates a defect produces false findings as reliably as one that understates it, and a stale comment outlives the code it describes.** Verification for the replacement claims is under D5a. | `*/app/logging_config.py` |
| D5d | `payment-service/app/payments.py::charge` logged the cardholder `name` in the clear at INFO (`redact_dict` passed it through -- `name` was not in `_SENSITIVE_KEYS`). Verified by executing the redactor: a charge for "Maria Gonzalez" wrote that string to `logs/payment-service.log`. | **Fixed.** Two independent guards, because each fails differently. (1) `charge()` no longer puts `name` in the logged object at all -- omitted rather than redacted, so there is nothing to redact, and it served no diagnostic purpose since a charge is correlated by `loan_id`, `last4` and `idempotency_key`. (2) `name` and `cardholder_name` are now in `_SENSITIVE_KEYS`, so a future call site cannot reintroduce the leak merely by including the field. Regression tests assert on **captured log output**, not on the object `charge()` builds -- checking the object would pass on a version that logged the raw values through a second, unredacted call, and logging is where this defect lived. Mutation-verified: removing both guards fails 4 of the 6 tests. One pre-existing test asserted the opposite (`assert "Robin Fictional" in out["name"]`) and was rewritten -- it encoded the very assumption this row disproves, that a cardholder name is safe to log. | `payment-service/tests/test_cardholder_name_not_logged.py`, `payment-service/app/redactor.py` |
| D5b | Full PAN/CVV persisted on the `payments` row. | **Fixed.** The columns are gone: `db/migrations/0031` dropped them from existing databases and `db/init/001_schema.sql` no longer creates them, so neither a migrated nor a freshly initialised database has a `payments.pan` or `payments.cvv` at all. The writers went first, in the order an expand/contract migration requires -- the application stopped writing them in PR #8 (ADR 0008), the seeds in PR #11, `0029` back-filled `last4` so payment history still displays, and `0031` refused to run until that back-fill was complete and an operator acknowledged the drop. Verified on a fresh volume: `payments` has no card columns, every card row carries `last4` (285/285; the 142 rows without are ACH), and `db/tools/check_no_pan_readers.py` exits 0. **Kept as history rather than deleted, because the shape of the mistake recurred:** this row twice claimed "no writer remains" while `002_seed.sql`/`003_seed_bulk.sql` were still inserting full PAN and CVV and a seeded audit row read `charge req pan=4111111111111111`. Review caught it both times, and the claim was only reinstated once the seeds had actually changed and a test held them to it -- `payment-service/tests/test_docs_match_the_logging_code.py` scans every seed INSERT's values and every seed target table, and scans a real `db/init` database, failing on a Luhn-valid card number in any column. | `db/migrations/0031_drop_payments_pan_cvv.sql`, `db/init/001_schema.sql`, `payment-service/tests/test_docs_match_the_logging_code.py` |
| **D6** | The origination fee constant had drifted to three independent copies, one of them `0.025` against a published 3.0%, breaching the Reg Z APR tolerance. This is the real Reg Z breach — not the arithmetic. | **Fixed** — one source of truth in `disclosure-service/app/fees.py` | `disclosure-service/app/fees.py` |
| **D7** | `reconciliation.peek` exposes two totals that do not tie out. It is not a control — nothing runs on a schedule and nothing alerts. | **CONTROL IMPLEMENTED / CURRENT PHASE DECIDED BY THE CLIENT: IN-APP ONLY / EXTERNAL DELIVERY OPS-BLOCKED AND CLIENT-PROHIBITED.** The first half of that label is new wording for an old fact, and the reason is the same as D24's: `ALERT DELIVERY OPS-BLOCKED` alone reads as though nobody has decided where findings should go. Somebody has. The client's decision of 2026-08-24 names the destination for this phase -- the in-app reconciliation queue -- and prohibits email, Slack, PagerDuty, SMS and webhooks before the freeze. So the current phase is settled and implemented; what has no destination is a *firing alert* with nobody looking, and that is the part still waiting on operations. The control itself is implemented and tested, and it is what Week 7's acceptance asked for (`docs/ROADMAP.md`, Week 7 status block): one instrumented path and one control, scheduled, with a break-report and an alert rule. What is missing is not code in this repository -- it is a destination for a firing alert: `app.reconcile_job` is a scheduled entry point whose exit code distinguishes clean (0), breach (1) and could-not-run (2); the comparison is **per transaction**, keyed on the processor's own settlement reference (`payments.processor_ref`, db/migrations/0041), so two wrong transactions on one loan cannot net to zero and each break names the capture responsible; a threshold (`RECONCILIATION_BREAK_THRESHOLD`, strict by default, falling back to strict on a bad value) decides breach; every run writes a `reconciliation_runs` row with counts, totals and the threshold it was judged against, so "when did this last agree?" is answerable; Prometheus gauges expose break count, break value and last-success time; both sides are scoped to the settlement file's own date range, so a daily file is not compared against lifetime totals; and the job fails closed when its run record cannot be written, so it can never report `ok` without durable evidence. **What is not built, and what each piece is waiting on. (1) Routing a FIRING ALERT to a human -- OPS-BLOCKED, and since 2026-08-24 also CLIENT-PROHIBITED.** `monitoring/alerts.yml` is loaded and its rules reach `firing`, and `db/tests/test_alert_rules_watch_metrics_that_exist.py` proves each rule watches a metric something actually emits -- but no Alertmanager is deployed, so nothing is routed to a person and a firing alert has to be looked at. That is not an omission this repository can close by writing code: a receiver needs an on-call owner and an escalation path, which is a deployment decision. Wiring a fake receiver to turn the row green would be the defect this control exists to remove. **A second reason now sits on top of the first, and both must lift:** the client's decision of 2026-08-24 prohibits email, Slack, PagerDuty, webhook and SMS delivery and authorises no new credentials or integrations before the freeze, so even with an on-call owner named nothing may be wired today. **What DID change on that date:** breaks are now visible to a human in-app, on `/reconciliation`, beside the run that found them and next to -- but clearly apart from -- the payment review candidates that are a different kind of finding (D22). So the sentence "nothing reaches a person" is no longer the whole truth and is not written that way: a staff member who opens the queue sees the breaks. What has no destination is an alert that fires while nobody is looking. **(2) Captures written before `db/migrations/0041` -- bounded and self-clearing, not open.** They carry no processor reference and cannot be matched, so they are reported as `unreferenced_capture` breaks rather than skipped: the control understates nothing and the set never grows, because every capture written after 0041 carries the reference. **(3) Scope of the comparison -- tracked as D2, not here.** The comparison covers processor-backed captures only — servicing's legacy `POST /payments` calls no processor, so no settlement line can corroborate it; those rows are labelled (`capture_source`, db/migrations/0042), excluded and counted on each run, which makes the gap visible but does not close it (that is D2). |  `servicing-service/app/main.py` |
| **D8** | Fee waiver / balance adjust is available to *any* authenticated user — no role check, no second approver, no ledger entry. | **Fixed.** All four controls the original sentence bundled now exist. **Network:** every money route requires `X-Internal-Token`, and the service refuses to start on a weak or unset one. **Ledger:** `db/migrations/0035` makes `balances` a projection of immutable entries. **The human:** the gateway signs a short-lived Ed25519 assertion naming the staff member and servicing verifies it against the public half — it cannot mint one, which is what makes its own role check meaningful. **The second person:** `adjust-balance` and `waive-fee` no longer move money at all. They raise a proposal (202, balance untouched); a **different** verified principal resolves it through `resolve_pending_movement`, which locks the row, permits exactly one transition, refuses self-approval including admin, revalidates the target inside the lock, and on approval writes exactly one ledger entry built from the locked proposal with the **approver** as actor. A rejection writes none and the proposal is retained. **Bounds, stated rather than implied:** the admin threshold, the maximum delta and the permitted loan statuses are cohort/demo configuration approved 2026-08-16, **not Lending Operations policy**, read from the environment with no defaults and failing the service's boot when absent; the maker's scope is spec 0002 REQ-VAL-14 option 2 (any staff principal on any serviced `current` loan) because this system has no staff-to-loan assignment, recorded as a reviewed limitation; and there is no notification, delegation or out-of-office routing (spec 0002 §8). **Evidence:** `db/tests/test_0037_resolve_pending_movement.py` (23 cases incl. a real two-connection approval race), `db/tests/test_0036_pending_movements.py` (41), `servicing-service/tests/test_maker_checker_api.py` (role matrix, refuse-at-creation, machine paths preserved). | `servicing-service/app/maker_checker.py`, `db/migrations/0037_resolve_pending_movement.sql`, `specs/0002-maker-checker-self-approval.md` |
| **D11** | KYC is CIP-only: no OFAC/sanctions screening, no UBO, no ongoing monitoring, no SAR path. | **Open** (deliberate scope limit, not an oversight). **Specified, not built:** `specs/0004-kyc-aml-ubo-and-sanctions-screening.md` and `adr/0012-sanctions-screening-integration.md` (Week 9) carry the `beneficial_owners` model, the screening-evidence design, the provider seam with its fail-closed and idempotency rules, the monitoring triggers and the SAR boundary. Building it is blocked on decisions this repository does not hold: **COMPLIANCE-BLOCKED** on which BSA/AML obligations bind Meridian, on sanctions-match disposition and on SAR rules; **VENDOR-BLOCKED** on provider selection and match-scoring semantics; **CLIENT-BLOCKED** on owner PII collection, ownership-chain depth and re-screening cadence; **OPS-BLOCKED** on who receives monitoring findings. `sanctions_screened` and `ubo_captured` remain hardcoded `False` so the gap stays visible, and no migration is written | `kyc-service/app/main.py`, `specs/0004-kyc-aml-ubo-and-sanctions-screening.md` |
| **D12** | Money columns stored as `DOUBLE PRECISION`, accumulating float error across the amortization loop. | **Fixed** — migrated to `NUMERIC`, Decimal arithmetic in `apr.py`/`offer.py`/`schedule.py` and `balance.py`/`delinquency.py`/`schedule.py` | `docs/runbook.md` |
| **D13** | Full PAN/CVV storage, PCI. Same defect as D5b, numbered separately in the original report. | **Fixed** -- see D5b, which carries the detail and the history. Short version: the writers went in PR #8 and PR #11, `0029` back-filled `last4`, and `0031` dropped the columns behind a gate that refused without a completed back-fill and an explicit operator acknowledgement. What this does NOT claim: PCI-DSS compliance. Removing stored card data closes this defect; a compliance position needs a QSA assessment and a real processor, neither of which exists in this training build. | `db/migrations/0031_drop_payments_pan_cvv.sql`, `db/init/001_schema.sql` |
| **D14** | No payment waterfall -- a payment reduced the balance directly instead of being applied fees -> interest -> principal. A borrower carrying a late fee had their payment reduce principal while the fee stayed owed and kept the loan delinquent. | **Fixed.** `balance.apply_payment_once` now splits a payment through `waterfall.allocate` and writes one `ledger_entries` row per component that moved. The order is not invented here: `policies/fee_schedule.md` publishes it as the source of truth (fees -> accrued interest -> principal). **Interest owed is DERIVED from the loan's stored contractual schedule** -- the amounts boarding copied from the signed offer -- less the interest already posted to the ledger, so no new state, no accrual job and no day-count convention were invented. Cents are exact: every amount is a Decimal quantized to cents, so allocation is subtraction and no residual can arise; a sub-cent input is refused rather than rounded into a balance. **Three bounds, each deliberate.** An OVERPAYMENT is refused (409) rather than absorbed -- what happens to the excess is a Lending Operations question no document here answers, and both available answers change what the borrower owes. A loan with NO stored schedule owes no derivable interest, so its payment goes to fees then principal; that under-allocates to interest rather than guessing, which is the conservative direction. A NEGATIVE amount owed (a fee credit, reachable via `waive_fee`) is clamped to zero rather than refused -- rejecting a borrower's payment because they hold a credit is worse than the state it guards. `balance.apply_payment` is NOT converted: it is dead code reached by no route, and belongs to ADR 0010's writer retirement. | `services/servicing-service/app/waterfall.py`, `services/servicing-service/app/balance.py`, `services/servicing-service/tests/test_payment_waterfall.py` (26), `services/servicing-service/tests/test_payment_waterfall_posts_components.py` (8), `policies/fee_schedule.md` |
| **D15** | `compute_apr` was not the actuarial method despite its docstring saying so — it used a simple add-on ratio, understating the disclosed APR by 4.39pp on the repo's own vector (5.196% against an actuarial 9.584%), 35× the Reg Z tolerance, in the direction that flatters the loan. Found by giving the tests an oracle that does not re-implement `apr.py`. | **Fixed** — actuarial IRR solve (Reg Z Appendix J); `test_apr.py` checks it against an independently-solved rate, a no-fee identity that needs no implementation, and hand-checkable arithmetic | `disclosure-service/app/apr.py` |
| **D16** | The disclosed finance charge excluded the origination fee (a prepaid finance charge under Reg Z), so the TILA box did not foot: amount financed + finance charge was short of total of payments by exactly the fee. | **Fixed** — finance charge computed as `total_of_payments - amount_financed`; the identity is asserted per vector | `disclosure-service/app/apr.py` |
| **D17** | The offer read path rebuilt the display amortization schedule at the **APR** rather than the note rate. The two differ once a prepaid fee exists, so the redisplayed schedule showed a monthly payment that did not match the disclosed one. Present before D15 too, in the other direction. | **Fixed** — the note rate is recovered from the stored payment (`note_rate_from_payment`) and the schedule is built at that | `disclosure-service/app/routers/offers.py` |
| **D18** | `logs/payment-service.log` was a tracked file until PR #9 and remains in `main`'s history. It contains card numbers and SSNs. **No real cardholder data:** all three PANs are the canonical published test numbers (Visa `4111…1111`, Mastercard `5500…5559`, Amex `3400…0009`) that appear in every processor's documentation; the SSNs are real-*format* but fabricated for fictional applicants. Recorded here so a reader who finds them in `git log` knows what they are rather than treating it as a breach. | **Untracked and gitignored** (PR #9). History deliberately **not** rewritten: a force-push to a shared `main` breaks every clone and open PR, and GitHub keeps the old commit reachable by SHA regardless — disruption without complete assurance, for data that is not sensitive | `git log -- logs/payment-service.log` |
| **D19** | `loans.apr` held the **note rate**, not an APR -- boarding writes the contractual rate there because servicing amortizes that column, and billing the disclosed APR would charge the borrower above their own disclosure (PR #10 review). The pre-change path wrote the DISCLOSED APR into the same column: 5.196% for a contract priced at 7.99%. So one column held two different regulated figures, and the servicing UI labelled it "APR". | **Fixed.** The column is gone. `db/migrations/0038` added `loans.note_rate_pct` and back-filled it only where the value could be PROVEN -- the offer's own proven `note_rate_pct` agreeing, or `schedule_version` showing the current boarding path wrote it -- leaving everything else NULL rather than relabelling a disclosed APR as a contractual term. `db/migrations/0039` then dropped `apr` and made `note_rate_pct NOT NULL`, behind two gates: it refuses while any loan has an unproven rate (naming the rows, and saying why copying `apr` across is the wrong repair), and it requires an explicit operator acknowledgement that no deployed image still reads the old column. Every reader and both boarding paths now use the new name, and no response field is called `apr` either. **Bound of the claim:** this fixes the NAME and the ambiguity; it recomputes no money, and the disclosed APR's own correctness is D6/D16, a different concern that shares a word. `offers.apr` is untouched and correctly named. Sequencing and the unproven-row decision are `docs/RUNBOOK-loans-apr-contract.md`. | `db/migrations/0038_loans_note_rate_expand.sql`, `db/migrations/0039_drop_loans_apr.sql`, `db/tests/test_0039_drop_loans_apr.py` (19), `db/tests/test_0038_loans_note_rate_expand.py` (10), `db/tests/test_note_rate_readers_agree.py`, `docs/RUNBOOK-loans-apr-contract.md` |
| **D20** | `check_no_pan_readers.py` decides whether anything still reads `payments.pan`/`payments.cvv` by folding SQL out of the syntax tree, and a static folder can always be defeated by a sufficiently dynamic program: a column name read from config, fetched from the database, or reached through `getattr`. It resolves literals, adjacent literals, concatenation, f-strings, `.format()` and `%` templates with their arguments, names bound earlier in the same scope, and joined sequences; it fails closed on projections it cannot finish. It does not evaluate arbitrary Python, and extending it until it does would replace a purpose-built check with a worse interpreter. | **Bounded, not fixed** — and deliberately still bounded after 2026-08-26, because the honest options were to strengthen what is provable or to enlarge the folder into a worse interpreter. The premise is enforced instead: `db/tests/test_payments_sql_is_static.py` asserts that no application code composes SQL against the `payments` table at all, and **as of 2026-08-26 that file also asserts the second premise the folder rests on: no application code reads `payments` with a wildcard, in any of its forms -- bare `SELECT *`, table-qualified `payments.*`, an alias bound to `payments` (`SELECT p.* FROM payments p`, including through a JOIN), or an **unqualified `SELECT *` whose relation set includes `payments`** -- `SELECT * FROM loans l JOIN payments p ON ...` reads every payments column while naming none, and both positional forms miss it**. Quoted identifiers are covered too -- `SELECT "p".* FROM payments AS "p"`, the joined form, and a quoted table name -- because a bare character class can never match them and PostgreSQL accepts them. The star check deliberately over-approximates in one direction (an outer `*` with `payments` only in a subquery is flagged) and excludes `count(*)`, which is an aggregate rather than a projection. That shape was already absent and nothing asserted it, which made it load-bearing and unguarded: a wildcard read names no columns, so the folder can prove the statement touches `payments` and nothing whatever about which columns come back -- the exact uncertainty this entry records, in a form no amount of folding resolves, while the checker still reports clean. Deliberately narrow: a ban on wildcard reads of the one table whose dropped columns the checker polices, not a general `SELECT *` style rule. Mutation-checked by planting a wildcard read in `payment-service/app/payments.py`, which fails it. What is still NOT closed, and why the status does not change: a column name reached through config, a database lookup or `getattr` remains outside static reach (currently 22 statements, all literals; the 4 composed ones in the tree are migration-test fixtures interpolating a schema name). While that holds, the folder never has to resolve anything interesting to be right about this repository, and each of the five evasion shapes raised in review is caught by a test rather than by the folder | `db/tools/check_no_pan_readers.py`, `db/tests/test_payments_sql_is_static.py` |
| **D21** | The client asked at the 2026-08-19 demo exactly what happens to the card security code (CVV) and the full card number (PAN) in the payment path. Column removal (0031) was already evidenced; the PATH was not — transient handling, logs, caches, and pre-migration backups had no stated answer. | **Traced and tested, with named boundaries — not closed.** `docs/PAN-CVV-DATA-FLOW.md` states the flow value by value: both are reduced in the browser by `frontend/lib/tokenize.ts` before any Meridian backend is called, so no Meridian **backend service** holds either even transiently — the frontend is a separate matter and is scoped as boundary B1 in the data-flow statement, because `frontend/lib/tokenize.ts` is Meridian code that takes both values as arguments; `PaymentIn`'s `extra="forbid"` refuses the fields by name, the other caller-controlled fields are shape-constrained so they cannot carry the same data, `processor_token` is used and never persisted, and reconciliation reads and records only `loan_id`/`processor_ref`/amounts. **Two claims in the first version of this entry were too wide and were corrected in review (PR #51):** `processor_token` was length-constrained only, so with a real processor configured a card-shaped token was posted verbatim in an outbound request body — now refused both at `PaymentIn` and inside `authorize_charge`, before either path builds a request, and asserted at the transport rather than on the exception; and the headline claim said no Meridian *process* receives the values, while `frontend/lib/tokenize.ts` is Meridian code that takes both as arguments. The claim is now scoped to backend services, with the frontend stated as an open boundary until a real hosted field replaces the mock. **Executed:** `payment-service/tests/test_pan_cvv_never_enter_the_payment_path.py` (44 cases) runs a synthetic capture and sweeps every SQL statement, every bound parameter and every log record for a Luhn-valid test PAN pushed through each allowed field in turn, and `servicing-service/tests/test_reconciliation_export_carries_no_card_data.py` (6) feeds a settlement file that genuinely carries a PAN column and asserts nothing of it survives into the comparison or the recorded run. Both include a guard-the-guard case, because a negative assertion over an empty surface passes against a system that leaks everything. **Open, and owned outside this repository: (a) pre-migration database backups, snapshots and dumps taken before `db/migrations/0031_drop_payments_pan_cvv.sql` still contain real `payments.pan`/`payments.cvv`, and nothing here can attest they were purged — an operations task with an operations owner; (b) reverse-proxy, container-runtime and platform logs are not configured in this repository and were not tested (same scope limit as D5a); (c) `frontend/lib/tokenize.ts` is a mock, not a PCI-attested tokenizer, so no claim transfers to whichever SDK replaces it; (d) CDN/proxy/browser caches, browser memory and autofill are outside anything this repository can assert. This is not a PCI assessment.** | `docs/PAN-CVV-DATA-FLOW.md`, `services/payment-service/tests/test_pan_cvv_never_enter_the_payment_path.py`, `services/servicing-service/tests/test_reconciliation_export_carries_no_card_data.py` |
| **D22** | Reconciliation does not detect a genuine double-fund: two captures that both settle against the same loan for the same amount inside a short window. Each has its own `processor_ref`, so each matches its own settlement line, and all four break kinds pass them — `settlement_only` and `ledger_only` need a reference on one side only, `amount_mismatch` needs the two sides of ONE reference to disagree, and `unreferenced_capture` needs a missing reference. Nothing compares two references to each other. The observed case at the 2026-08-19 demo is exactly this shape. Idempotency (D2) closes the retry/double-click path; it does not close two genuinely separate submissions, and the control that would notice afterwards is this one. | **CLIENT DECISION RECEIVED 2026-08-24 — DETECTION AND REVIEW QUEUE IMPLEMENTED. No longer CLIENT-DEFERRED.** <br>**The three questions this entry held, and the answers given.** (1) Should two settled captures on one loan for the same amount inside a short window raise a break? **No — and that is the part worth reading twice.** The client's answer was not the fifth break kind proposed below; it was to FLAG the payment for human reconciliation review and never to treat the flag as a duplicate conclusion, a validity conclusion, or permission to move money. (2) What is the window? A rolling **30 minutes**, inclusive, for the heuristic signal; the exact signals carry **no** window — a repeated provider transaction id or idempotency key qualifies regardless of elapsed time. (3) What false-positive appetite? Settled by requiring **four** factors together for the heuristic — same loan AND amount AND payment source AND channel — so the legitimate-repeat-payment shape this entry worried about does not qualify on loan and amount alone. <br>**What was built.** `services/payment-service/app/review_signals.py` raises the two signals at capture time and cannot fail a payment while doing it; `db/migrations/0044` adds `payments.source_ref`, an opaque non-PAN-derived funding handle, because nothing existing could prove "same payment source"; `db/migrations/0045` holds `reconciliation_review_items` with the signal vocabulary and the client's three dispositions as CHECK constraints and a write-once trigger; `services/servicing-service/app/review_queue.py` plus `/reconciliation` are the in-app queue a human reads and answers, staff-only behind a verified signed principal. Dispositions are exactly `confirmed_duplicate`, `legitimate_distinct_payment` and `requires_further_review`. <br>**What the follow-up below said, and why it was not done that way.** It called for "a fifth break kind in `servicing-service/app/reconciliation.py::compare` keyed on (loan, amount, window) across references". That is superseded: a break is the control asserting the books are wrong, and the client asked for a question put to a person instead. **Reconciliation therefore still raises no break on this shape, and that is now correct rather than pending** — which is why `services/servicing-service/tests/test_double_capture_is_not_detected_yet.py` still passes and was not turned round. Its own docstring now says so, so a reader does not take a passing tripwire for an undetected defect. The other two follow-ups were delivered in the review-signal form: the seeded double-capture shape is covered, and the false-positive case is asserted directly — a second installment matching on loan and amount but not on source or channel raises nothing. <br>**What remains, and it is not this item.** Routing a *firing Prometheus alert* to a person is still unresolved and is tracked at **D7**, where it now carries two reasons rather than one: no Alertmanager owner (OPS-BLOCKED) and, since 2026-08-24, a client prohibition on every external channel before the freeze. The in-app queue is the authorised destination and it exists; an external page has none and may not be wired. | `services/payment-service/app/review_signals.py`, `services/servicing-service/app/review_queue.py`, `db/migrations/0045_reconciliation_review_items.sql`, `docs/ROADMAP.md` |
| **D24** | The client's synthetic offline fairness-evaluation package was not supplied for three days after the decision that referred to it, so the only permitted form of fairness evaluation could not be run. Client decision, 2026-08-24: no real protected-class collection, no approved proxy, ZIP/ZIP3 prohibited as one, and synthetic labels permitted **only** inside an isolated offline fixture. The runtime ZIP3 screen was retired the same day. | **POLICY ANSWERED / CLIENT SYNTHETIC TRAINING PACKAGE RECEIVED 2026-08-24, INGESTED 2026-08-27.** Both halves are now closed and neither is a production claim. *This cell read `POLICY ANSWERED / ARTIFACT PENDING -- CLIENT-PROVIDED-FIXTURE-NOT-PRESENT` until 2026-08-27; the package arrived as a client email attachment and the absence claim, correct when written, expired.* **What arrived:** `Kalabe-Synthetic-Vendor-Governance-Client-Inputs-Only-2026-08-24.zip`, version `CCUS-SYN-2026.08.24` -- a synthetic vendor profile, a 12-code reason taxonomy, approved consumer wording, a synthetic model card, validation and fairness summaries, three client policies, 28 acceptance cases, 14 negative fixtures and a 32-row audit-only fairness fixture. Ingested byte-for-byte at `fixtures/offline_fairness_training/client_package_2026-08-24/` with all **34 checksums verified**; `db/tests/test_client_package_is_byte_preserved.py` re-verifies them every run, and `.gitattributes` disables newline conversion for that directory because without it a Windows checkout fails all 34 silently. The raw `.zip` is deliberately not committed. **What it authorises:** the offline evaluation, and only that -- `db/tools/offline_fairness_eval.py` reports aggregate counts and rates and computes **no verdict**, because the client's fairness-data policy permits no production or real-world fairness claim and their EVAL-16 rejects that claim by name. `db/tools/governance_acceptance.py` executes all 28 acceptance cases against the mapping and refusal rules: **28 resolved, 0 failed, nothing delegated**. An earlier version delegated nine cases to existing containment tests by category and review found two of them unenforced -- a discriminatory sentence returned unchanged because it is specific rather than generic, and vendor text the runtime injection guard does not match -- while the report still read 0 failed. The delegation table is gone and a test asserts it cannot return. **What it does not authorise:** anything at runtime. Their own precedence policy places this packet in the lowest tier, below current law and below vendor-issued approved documents, of which none are identified. The twelve `CCUS-*` codes are **not** wired into `decision-service`: the stub scorer does not emit them, so mapping its two internal reasons onto this taxonomy would be nearest-match substitution, which their adverse-action boundary prohibits by name. `APPROVED_CONSUMER_REASONS` is unchanged. **Never claimable from it:** that the model is fair, that it is production validated, that it is approved for real consumer decisions, or that vendor governance documentation exists. Real approved vendor materials must still replace this packet before any non-training use | `fixtures/offline_fairness_training/README.md`, `fixtures/offline_fairness_training/client_package_2026-08-24/`, `db/tests/test_client_package_is_byte_preserved.py`, `specs/0003-fair-lending-monitoring.md` |
| **D23** | The late fee priced off `balances.past_due` -- a single projected total that mixes principal, interest and every fee ever assessed -- and a posted fee RAISES that total, so repeated assessment compounded. The client asked directly at the 2026-08-19 demo whether that was intended. *This entry originally framed the question as compounding alone; the answer supplied on 2026-08-29 replaces the rule outright, so what is recorded here now is the decided rule rather than the original question.* | **PARTIALLY IMPLEMENTED (PR #143) / RUNTIME CUTOVER CLIENT-BLOCKED.** This column previously read "IMPLEMENTATION REQUIRES DATA-MODEL EXPANSION" and stated, as present-tense fact, that nothing records which installment a fee belongs to and that no `installment`, `period` or `due_date` column exists anywhere in `db/`. Both stopped being true when #143 merged on 2026-08-31. The rule is unchanged and the RUNTIME is unchanged; what changed is that the primitive the rule needs now exists, so the ask is no longer "expand the data model" but "decide two policy questions". <br>**The decided rule, unchanged.** At most ONE late fee per missed scheduled installment, assessed only after the existing grace period, never reassessed against the same installment; a later scheduled installment that separately becomes overdue may receive one fee of its own. The amount is `min($35.00, 5% x unpaid scheduled PRINCIPAL + INTEREST for THAT installment)`. Previous late fees and every other fee are excluded from the percentage base. Worked examples supplied with the decision: unpaid scheduled P&I of $200 -> $10, $500 -> $25, $700 -> $35 (cap), $1000 -> $35 (cap). <br>**WHAT THE RUNTIME DOES TODAY -- still the superseded rule, and this is the sentence to read first.** `assess_late_fee` (`servicing-service/app/delinquency.py`) reads `balances.past_due` under `FOR UPDATE` and writes a `fee_assessed` entry priced by `late_fee_for(past_due)` = `min($35, 5% of past_due)`. `past_due` is exactly the mixed total the decision excludes as a base, a posted fee raises it, and nothing in that path consults an installment. A borrower today is charged under the rule the client replaced. That is knowingly wrong against the decided rule and is the open half of this row. <br>**IMPLEMENTED (#143), each verifiable in the tree rather than asserted here.** *(1) Installment identity.* `servicing-service/app/installments.py` derives the schedule from the stored contract -- `installments_for`, `installment`, `overdue_installments` -- anchored on `loans.opened_at` and billed from `amortization_from_contract` rather than recomputed. *(2) The column.* `ledger_entries.installment_no` (`db/migrations/0046_ledger_installment_no.sql`, and in `db/init/001_schema.sql` so a fresh volume matches). Nullable, no default, no backfill. *(3) One fee per installment, enforced by the database.* Partial unique index `ledger_one_late_fee_per_installment ON ledger_entries (loan_id, installment_no) WHERE entry_type = 'fee_assessed' AND installment_no IS NOT NULL` -- not an application check, so a retry and a concurrent second assessor are both refused by construction. *(4) Real bound validation.* `ledger_installment_is_in_the_loan_schedule()` refuses an installment outside the loan's term, reading the loan row `FOR SHARE`. *(5) Scheduleless loans refused.* The same trigger refuses a citation when the loan carries no complete stored contract group (`schedule_version`, `regular_payment`, `regular_payment_count`, `final_payment`), rather than inventing a schedule for it. *(6) The contract cannot move under a citation.* `loans_contract_is_frozen_once_cited()` refuses a change to `term_months`, `schedule_version`, `regular_payment`, `regular_payment_count`, `final_payment`, `note_rate_pct` or `opened_at` on a loan whose ledger already cites an installment -- scoped to CITED loans so seeding and correction of uncited rows still work. *(7) Both races proved with genuinely overlapping sessions*, not simulated: `test_two_concurrent_assessors_cannot_both_write_one_installment` and `test_the_contract_cannot_change_under_an_in_flight_fee` (`db/tests/test_0046_one_late_fee_per_installment.py`) assert the second writer is still BLOCKED before the first commits. *(8) The decided arithmetic exists.* `late_fee_for_installment()` returns `min($35, 5% x unpaid scheduled P&I)` quantized ROUND_DOWN, matching all four worked examples. *(9) No historical backfill, deliberately.* The migration writes no `installment_no` onto any existing row. Every fee assessed before this change was assessed under the arrears rule and belongs to no installment; labelling those rows would manufacture a fact nobody recorded. <br>**NOT IMPLEMENTED, and none of it is blocked on the data model any more.** *(i) The runtime cutover.* `assess_late_fee` still follows the arrears rule; nothing calls `late_fee_for_installment()` from the money path. *(ii) Payment-to-installment attribution.* `payment_applications` is still `(payment_id, loan_id, amount, applied_at)` and `ledger_entries` still records a signed delta per `component`; neither carries a period. Money is tracked per loan and per component. *(iii) Unpaid scheduled P&I once any payment has been applied.* Follows from (ii): `installments.unpaid_scheduled_pi()` raises `InstallmentAttributionUnknown` naming this row whenever a payment exists, rather than picking an order and returning a number nobody decided. <br>**CLIENT-BLOCKED -- exactly two questions, and this is why the runtime is not cut over.** *(1) The exact grace period.* No grace period exists anywhere in this repository. Searched exhaustively rather than assumed: the vendor `policies/fee_schedule.md` at `a726640` carried none either, and `git log -S"grace_days"` across all history returns only #143's own commit. `overdue_installments()` therefore takes `grace_days` as a REQUIRED argument with no default, so a caller must supply a number the client owns. *(2) Payment allocation order across overdue installments.* `adr/0010` states the allocation order for D14's waterfall is deliberately out of scope. Oldest-unpaid-first is the natural reading of the client's own rule and is written down in no spec, so it stays uninvented. <br>**What would close this row:** those two answers, then a cutover PR that prices from `late_fee_for_installment()`, attributes payment components to installments, and switches `assess_late_fee` over -- with the arrears path removed rather than left beside it. | `db/migrations/0046_ledger_installment_no.sql`, `db/init/001_schema.sql`, `servicing-service/app/installments.py`, `servicing-service/app/delinquency.py`, `servicing-service/app/schedule.py`, `servicing-service/app/waterfall.py`, `db/tests/test_0046_one_late_fee_per_installment.py`, `policies/fee_schedule.md`, `adr/0010-append-only-ledger-for-servicing-balances.md` |
| **D25** | The landing page carries three compliance claims inherited from the Halcyon baseline -- **SOX-controlled**, **PCI compliant**, **ECOA / Reg B** (`git blame`: c56240f, 2023-11-02, with the vendor's own comment "Texture: the platform over-claims its compliance posture"). Every current authority contradicts the literal text: `README.md` says to treat any prior PCI-DSS compliance claim as false and calls SOX / ECOA-Reg B process claims beyond the decision audit trail unverified; `ARCHITECTURE.md` says nothing here asserts regulatory compliance; and `docs/presentations/2026-08-25-agentic-client-handoff.md` -- the newest client-facing direction -- lists "PCI compliant" under **Claims we must NOT make**. Rendered bare, the first screen of the demo contradicted the handoff document. **Evidence, per claim:** PCI **none** (a QSA and a real processor are both absent; the dropped card columns close a defect, not a compliance question); SOX **none** (`audit_logs` is written from one call site, none of them money-moving, and carries a `deleted_at` column); ECOA/Reg B **partial** -- `decision_events`, input-derived reason codes and `consumer_adverse_action_reason` are real, but an unqualified badge overstates them. | **Labelled, not removed.** No spec or ADR requires the badges and none mentions the landing page, so they are inherited copy rather than a requirement -- but they are also the clearest artifact of the platform over-claiming, which is worth keeping visible. A qualifier now renders beside them: *"Inherited vendor claims -- not verified by Meridian"*. `frontend/e2e/inherited-compliance-claims.spec.ts` pins the relationship rather than the text: if a claim is shown the qualifier is shown, and removing the claims entirely passes. **Removal remains a legitimate future remediation** and needs no change to the guard. | `frontend/app/page.tsx`, `frontend/app/globals.css`, `frontend/e2e/inherited-compliance-claims.spec.ts` |

## Review findings

| ID | What | Status | Cited at |
|---|---|---|---|
| **RF-1** | A missing or unreachable licensed model must fail closed rather than fall back to a stub score that could be mistaken for a real vendor response. | **Fixed** (PR #3) — `ModelUnavailableError`, `-stub` suffix on any stub `model_version` | `adr/0006-adverse-action-reason-mapping.md` |
| **RF-13** | Float-money test failures in disclosure-service and servicing-service, which CI had been routing around with a "known-flaky" split. | **Fixed** — see D12; the split is gone and all eight services run in one blocking job | `.github/workflows/ci.yml` |
| **RF-18** | `decisions` is `(app_id, outcome)` with no explanation for any application, and the RAG corpus dump carried raw `ssn`/`pan` on every record. | **Fixed** — `decision_events` (see D4) and corpus hygiene (ADR 0005) | `adr/0005-rag-corpus-hygiene.md` |
| **RF-24** | The Playwright suite shares one database across specs and has no per-spec isolation, so a full run fails specs that pass when run alone -- and a *different* set each run. **Re-measured 2026-08-26 at main `bfe623f`**, with both known confounders excluded (gateway rate limit raised to 2000 via `docker-compose.e2e.yml`, `DATABASE_URL` on `127.0.0.1` so the IPv6 drop cannot fire). Two full runs at `--workers=4`: **72 failed / 50 passed**, then **46 failed / 76 passed** -- a different set each time, which is the signature this entry describes. The failure profile is partly known, and stated no more strongly than the evidence allows. The largest single error class is `page.goto` timing out -- 24 of the second run's 46 failures -- which is **consistent with saturation**: one Next.js server and one backend stack serving four concurrent browsers inside a 30s budget. That is an inference from an error distribution in one run, not a measured cause: no CPU, connection-count or response-time data was collected, and a plurality is not a mechanism. What follows from it is correspondingly narrow: **per-spec database isolation would not address the `page.goto` timeout subset**, so isolation work alone cannot be assumed to close this row. It does not follow that shared state is irrelevant -- the remaining 22 failures were not classified, and three specs are known to select the same seeded loan while one of them mutates its `past_due` (`fee-waiver-clarity`, `staff-balance-label`, `adjustment-preview`, all `ORDER BY b.loan_id LIMIT 1`). Closing this row needs the failures classified rather than a fix chosen from the first plausible cause. Observed on PR #18 while producing E2E evidence: three separate full runs failed three different subsets, while the affected specs passed 6/6 in isolation. This makes a full-suite E2E result unusable as a pass/fail gate until the specs seed into isolated state. | **Open** -- not caused by, and not worsened by, PR #18; recorded so the flaky residue is tracked rather than explained away in a PR description | `frontend/e2e/`, `frontend/playwright.config.ts` |
| **RF-26** | Three test files hand-write their own `CREATE TABLE applications` instead of building from `db/init/001_schema.sql`: `test_access_token_lifecycle.py`, `test_decision_attempt_real_postgres.py` and `test_offer_repair_real_postgres.py`. Every column added to the real schema breaks them with `UndefinedColumn`, and the failure points at the test harness rather than the change. Migrations 0038 and 0039 each cost a round of this. `db/tests/migration_paths.py` already shows the fix -- build from the real files -- and the same shape caused the synthetic migrated-schema finding on PR #30. | **Open** -- pre-existing, not worsened here; the columns were kept in step rather than the harness rebuilt, which is a larger change than a retry-contract PR should carry | `services/origination-service/tests/`, `services/disclosure-service/tests/`, `db/tests/migration_paths.py` |
| **RF-27** | `fee-waiver-clarity.spec.ts` reserves an untouched serviced loan per test from a band past the ids the rest of the suite reaches (PR #113), and SKIPS any loan already carrying a ledger entry or a pending movement. That is what makes the file re-runnable -- the ledger is append-only, so a loan it has used cannot truthfully become untouched again and the fixture takes a fresh one instead of pretending to give one back. The band is therefore CONSUMED: roughly fifteen repeated local runs against the same persistent database exhaust it and the spec fails with `no untouched serviced loan left in the reserved band -- reseed the database`. Observed twice during the pre-freeze audit. **CI is unaffected** -- every run starts from a fresh volume via `db/init`, so the band is never partly consumed there. **Runtime is unaffected**: this is test fixture allocation, not application behaviour, and no money path reads the band. | **TEST-INFRA / LOCAL REPEATABILITY.** Workaround is the message's own instruction: `docker compose down -v` and start again, which reseeds. Not fixed during freeze -- it is developer ergonomics with no correctness, CI or client impact, and the error names the remedy rather than failing obscurely. Preferred direction afterwards, in order of appeal: an ephemeral E2E database per run; a per-test synthetic loan boarded through the real flow; or a per-run namespace. **Do NOT restore the previous save/restore fixture** -- it wrote `balances.past_due` directly, which is the ledger's projection, and its restore rewrote history the ledger had already recorded. Any solution must preserve append-only truth. | `frontend/e2e/fee-waiver-clarity.spec.ts`, `frontend/e2e/README.md`, `frontend/e2e/fixtures.ts`, `frontend/playwright.config.ts`, ADR 0010 |
| **RF-28** | Policy Chat is reachable by two different audiences depending on which half you look at. The gateway allows it **anonymously** and says so deliberately -- `assistant_policy_chat` records that policy Q&A carries no per-applicant financials or risk tier, so a borrower may ask without an account, the same anonymous-allowed pattern `/los/*` uses. The browser page is **staff-gated**: `/policy-chat` wraps itself in `RequireRole allow={csr, underwriter, admin}`. So the API answers a borrower and the screen refuses them. | **PRODUCT DECISION REQUIRED -- runtime behaviour deliberately unchanged on both sides.** <br>**Neither half is a bug, which is why this is a decision rather than a fix.** `docs/ROADMAP.md` describes loan-assistant as "Policy Q&A (**anyone**) + AI application summary (staff only)", which matches the gateway. The same file's demo walkthrough says to log in as `csr`/`underwriter`/`admin` and visit `/policy-chat`, which matches the page. Those two sentences are about different things -- one is the API's audience, the other is where the screen lives -- so both are literally true and **neither answers whether a borrower should be able to use Policy Chat in a browser**. <br>**Why nothing was changed.** Loosening the page would widen access to a feature on a guess about who it is for. Tightening the gateway would remove access the gateway granted on stated reasoning. Both are security-relevant edits with no authority behind them, and the cheapest wrong move here is the one that looks tidy. <br>**What IS true today, so nobody has to reconstruct it:** an anonymous or borrower caller can POST `/assistant/policy-chat` through the gateway and get an answer; the same person visiting `/policy-chat` is refused by `RequireRole`. The rate limiter and loan-assistant's own `MAX_INPUT_TOKENS` cost guard apply either way, so the open API path is bounded rather than unprotected. <br>**The question for the client:** is Policy Chat a staff tool, or a borrower-facing one? If staff-only, the gateway route should require a staff principal and its comment should stop saying a borrower may ask. If borrower-facing, the page's `RequireRole` should go and the navigation needs to offer it to a borrower, which it currently does not. Each answer is a small change; guessing which one is not. <br>**Impact while open.** No data is exposed either way -- the corpus is published lending policy and the answer path grounds every reply in it or refuses. What is wrong is that the product cannot say who the feature is for, and a borrower who was told about it would find the page refuses them. | `services/gateway/app/main.py` (`assistant_policy_chat`), `frontend/app/policy-chat/page.tsx`, `docs/ROADMAP.md` |
| **RF-25** | Staff manual-review reasons are persisted verbatim and returned as `adverse_action_reason`, so an underwriter can deny a referred applicant for "DTI still above policy" — a criterion the SYSTEM does not compute (adr/0007, G-DTI). Unlike a model-authored ratio, a human one may be legitimate: an underwriter who has read pay stubs and a credit report can compute DTI from documents this system never stores, and the existing test reason is literally "DTI recalculated under 43% with updated income". Blocking it would forbid an accurate Reg B reason; allowing it records an unsupported one. | **FIXED 2026-08-31 (#146 schema, #151 API, #152 UI) -- FOR THE QUESTION THIS ROW ASKS, WHICH IS NOT THE WHOLE OF THE PROBLEM.** RF-25 asked whether staff may apply DTI manually and what evidence must be attached. That is answered and built. The *adjacent* gap this row's own analysis surfaced -- a free-text manual-review reason naming DTI still reaching `adverse_action_reason` with no figures behind it -- is **RF-29**, open, and was deliberately given its own row rather than left as a caveat inside a closed one. A register a scanner reads as closed while its prose says otherwise is a register read two ways. <br>The entry below is kept as the record of what was decided and why the shape is what it is; what changed is that it is now implemented rather than specified. <br>**The decision, unchanged.** Manual DTI on a REFERRED application only, by an underwriter or an admin only, from approved SYNTHETIC source documents only; income, obligations, source-document references, the calculation, staff identity, role, timestamp and reason; a bare percentage explicitly insufficient; and the constraint that governs everything: a manual DTI is HUMAN-REVIEW EVIDENCE and must not approve, deny, override, mutate a decision or trigger model output. <br>**What closed each gap this entry identified.** *(e) the figures and the calculation* -- `manual_dti_assessments` stores both inputs as `NUMERIC` and a `dti_bp INTEGER`, with `CONSTRAINT manual_dti_is_reproducible CHECK (dti_bp = round(monthly_debt_obligations * 10000 / gross_monthly_income))`, so the ratio is reproducible BY THE DATABASE rather than asserted by whoever wrote it. The API never computes it either -- the INSERT hands Postgres the two inputs and lets it evaluate the same expression the CHECK verifies, because Python rounds half-to-even where Postgres rounds half-away-from-zero and a second copy of the formula disagrees at exactly .5bp. *(f) source-document references, which this entry called the larger half and correctly said needed something to refer to* -- `manual_dti_source_documents` is that registry: a small purpose-built table of approved SYNTHETIC references with `CHECK (is_synthetic)` so a row claiming to be a real applicant's paystub cannot be written, `approved` defaulting to FALSE, and one deliberately unapproved seed row so the refusal path has something real to refuse. No document CONTENT is stored or ingested anywhere: no OCR, no extraction, no embeddings, no external call, no file storage, and `test_the_route_accepts_no_document_content_at_all` parses the router with `ast` to keep it that way. *(g) the shape problem* -- the evidence lives in its own append-only table, never in `manual_reviews`, so recording it cannot consume the single final-decision slot. <br>**The rule that governs it, enforced rather than intended.** Referred-only, the role HELD rather than the role claimed, an active account, and approved documents are all checked by triggers that hold row locks while they check -- `manual_dti_is_permitted` locks `applications` then `decisions` then `users`, in the order the staff review path already acquires them, and `FOR SHARE` rather than `FOR UPDATE` so two reviewers can assess concurrently while an approve/deny cannot land underneath them. At-least-one-document is a DEFERRABLE INITIALLY DEFERRED constraint trigger, so a route may write the row and its links in either order and is still refused if it cites nothing. <br>**The required tests this entry recorded in advance all exist**, and the one it called the one that matters most is asserted three ways: `test_recording_evidence_changes_no_decision_surface` reads `decisions`, `applications.status`, `manual_reviews` and `decision_attempts` before and after a successful POST with a negative control so it cannot pass by writing nothing; the browser spec does the same through the real gateway; and no decision path reads the table. <br>**The backfill instruction was followed.** Nothing was derived from existing `manual_reviews.reason` strings -- the seeded "DTI recalculated under 43% with updated income" among them. Deriving structured evidence from a sentence somebody typed would manufacture the evidence this control exists to require. <br>**What this does NOT close, tracked as RF-29 rather than described here.** An underwriter can still deny a referred applicant with a free-text reason naming DTI, and that reason still reaches `adverse_action_reason` with no figures behind it. Nothing in this work changes `manual_reviews`. See RF-29. | `db/migrations/0047_manual_dti_evidence.sql`, `services/origination-service/app/routers/manual_dti.py`, `services/origination-service/app/staff_auth.py`, `frontend/app/underwriting/[appId]/ManualDtiPanel.tsx`, `db/tests/test_0047_manual_dti_evidence.py`, `services/origination-service/tests/test_manual_dti_api_real_postgres.py`, `frontend/e2e/manual-dti-evidence.spec.ts`, `adr/0007-underwriting-policy-dti-fraud-gap.md` |
| **RF-29** | A free-text manual-review reason naming DTI still reaches the applicant as `adverse_action_reason` with no figures, no documents and no reproducible calculation behind it. The seeded example is literally "DTI recalculated under 43% with updated income". | **OPEN -- and narrower than it was, which is why it is now its own row.** Split out of RF-25 on 2026-08-31 (PR #155): RF-25 asked whether staff may apply DTI manually and what evidence must be attached, and that is built (#146, #151, #152). This is the different question RF-25's analysis surfaced and could not answer: what an applicant is TOLD. <br>**Why it was not closed in passing.** The obvious move -- have the adverse-action reason cite a recorded `manual_dti_assessments` row -- is precisely what the client's constraint forbids: a manual DTI is human-review evidence and must not approve, deny, override, mutate a decision or trigger model output, and an adverse-action reason is part of the decision communicated to the applicant. Linking them would make the evidence decision-affecting by the back door. <br>**What it therefore needs, and does not have.** A client answer to a question nobody has asked yet: when a human reviewer has computed a DTI from documents and denies on it, what may the applicant be told, and must the notice's figures be reproducible from stored evidence? `adr/0007` (G-DTI) records that the SYSTEM computes no DTI, which is unchanged; this is about what a PERSON may assert in a Reg B notice. Not a data-model gap any more -- the evidence is representable now -- which makes this a policy question with an implementation waiting, rather than the other way round. <br>**Impact while open.** Unchanged from what RF-25 recorded: a denied applicant can receive a reason naming a ratio that nothing in the record substantiates. | `services/origination-service/app/routers/applications.py` (manual review insert), `db/migrations/0018_manual_reviews.sql`, `adr/0007-underwriting-policy-dti-fraud-gap.md`, `docs/DEBT.md` RF-25 |
| **RF-21** | The embedding provider needed the same swappable-client shape planned for the credit bureau, so a vendor can be substituted without touching retrieval or eval code. | **Fixed** — that bureau client now exists as `decision-service/app/bureau.py` (`BureauClient` Protocol) | `adr/0005-rag-corpus-hygiene.md` |

## Platform and perimeter security

Added 2026-08-26 after an end-to-end pass across authentication, browser
session, transport, container runtime, dependency posture and synthetic-data
hygiene. Every row below was verified against `main` at
`91b7f9ff4ca0bce972e5b989ca4ae181c8749194`, not carried over from an earlier
report.

**Why these are numbered separately.** The `D`/`RF` rows above came out of
money-path and AI-path reviews, and none of them covers the perimeter. Several
items below were already known — `gateway/app/auth.py`'s own docstring names
three of them as deliberate brownfield caveats, and all three are mapped here:
unsalted hashes (**SEC-01**), non-rotating tokens (**SEC-02**) and downstream
trust in the forwarded `X-User-Role` (**SEC-16**) — but a caveat in a code comment
is invisible to the register-driven planning everything else here uses. That gap
in the *register* is what this section closes; most of the findings themselves
are appropriate for a synthetic training build and are labelled as such rather
than as things to fix before a demo.

**No severity ranking here either**, for the reason given under "Not in this
register": ordering these would need a data-flow audit nobody has done. The
status column says what is true instead.

| ID | What | Status | Non-training requirement | Evidence |
|---|---|---|---|---|
| **SEC-01** | Password hashing is unsalted SHA-256 (`hash_password` returns `hashlib.sha256(...).hexdigest()`), so identical passwords produce identical digests and offline cracking is unimpeded. | **TRAINING-SCOPE ACCEPTED LIMITATION / OPEN BEFORE NON-TRAINING USE.** Already named in the module docstring as a deliberate brownfield caveat. Not changed here, because replacing it is not a patch but a migration: every seeded credential needs rehashing or a reset path, plus a window where both formats verify. No approved KDF choice exists and none is invented here. | A salted memory-hard KDF chosen by an authority rather than by a developer, with a documented migration and reset path | `services/gateway/app/auth.py:36` |
| **SEC-02** | Session tokens never rotate. A token is minted at login (uuid4 hex, stored in Redis with a TTL) and stays valid until TTL or logout; no rotation on privilege change, no refresh model. | **TRAINING-SCOPE ACCEPTED LIMITATION / NEEDS RUNTIME VERIFICATION.** Narrower than it sounds: logout *does* invalidate server-side, so this is absence of rotation rather than absence of revocation. Not bolted on, because rotation needs its semantics defined first — when it rotates, what happens to concurrent sessions, what a privilege change does. | A defined session lifecycle before rotation is implemented | `services/gateway/app/auth.py:98`, `:112` |
| **SEC-03** | CORS is `allow_origins=["*"]` with `allow_methods=["*"]` and `allow_headers=["*"]`. | **TRAINING-SCOPE ACCEPTED LIMITATION / NEEDS RUNTIME VERIFICATION**, and narrower than it looks: `allow_credentials` is **not** set and this app authenticates with a bearer header rather than a cookie, so a foreign origin cannot ride an existing session. It would still let any origin call the API with a token it already held. | An origin allow-list at deployment | `services/gateway/app/main.py:62` |
| **SEC-04** | The bearer token is kept in browser `localStorage`. | **TRAINING-SCOPE ACCEPTED LIMITATION**, recorded as latent rather than live: a sweep of `frontend/app`, `components` and `lib` found no `dangerouslySetInnerHTML`, no `innerHTML` assignment and no `eval`, so the current code has no script-injection path to exfiltrate it through. It becomes live the moment one appears. | A decision on token storage — httpOnly cookie plus CSRF defence, or in-memory with a refresh | `frontend/lib/api.ts:18`, `:34` |
| **SEC-05** | No Content-Security-Policy and no security response headers (`X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`). `next.config.mjs` sets `reactStrictMode` and `output: standalone` only. | **DEPLOYMENT HARDENING / OPEN / NEEDS RUNTIME VERIFICATION.** Deliberately not added blind: a CSP that breaks the app during demo week is worse than none, and the Google-Fonts plus inline-style surface needs measuring before a policy is written. | A measured CSP, added with a browser regression test | `frontend/next.config.mjs` |
| **SEC-06** | No `USER` directive in any of the nine Dockerfiles and no `user:` override in compose, so containers run as the base image's default user — root for `python:3.12-slim` and `node:20-alpine`. | **DEPLOYMENT HARDENING / OPEN.** Verified per file rather than asserted: all nine were checked individually and none declares a `USER`; `docker-compose.yml` contains zero `user:` keys. | A non-root runtime user per image, with file ownership adjusted | every `services/*/Dockerfile`, `frontend/Dockerfile`, `docker-compose.yml` |
| **SEC-07** | Postgres is published to the host on `5432`. | **LOCAL DEVELOPMENT EXPOSURE / INTENDED.** The browser E2E suite and `db/tests` both connect from the host, so the training setup requires it. Not a production posture and not claimed as one. | No host publication in a deployed environment | `docker-compose.yml:11` |
| **SEC-08** | Redis is published to the host on `6379`. | **FIXED 2026-08-31 (PR #148) — AND VERIFIED BY RUNNING THE STACK, not by reading the file that changed.** The publication is gone entirely rather than narrowed to loopback: the row's own premise — nothing on the host connects to it — was checked before removing it, and a repository-wide search for `localhost:6379` still returns nothing outside the guard that forbids it. With the stack up, `docker compose ps` reports `redis 6379/tcp` with no host binding, a socket probe of `127.0.0.1:6379` is refused exactly as a port nothing has ever listened on is, and services still reach it in-network (`redis-cli PING` → `PONG`, and the gateway's own `redis://redis:6379/0` → `True`). Postgres kept a publication because `db/tests`, the browser suite and CI's e2e job all connect from the host — narrowed to `127.0.0.1` rather than every interface. `db/tests/test_local_topology_does_not_publish_more_than_it_needs.py` holds both rules through one function, so the base file and the e2e overlay cannot drift apart. | Done | `docker-compose.yml`, `db/tests/test_local_topology_does_not_publish_more_than_it_needs.py` |
| **SEC-09** | Redis has no authentication: `REDIS_URL=redis://redis:6379/0` with no password, and no `requirepass` anywhere in compose or `.env.example`. | **STILL OPEN — but the sentence that made it urgent is no longer true, and saying so is the point.** This row read "combined with SEC-08 it is reachable from the host". Since PR #148 it is not: Redis has no host publication at all, verified with the stack up (see SEC-08). The store remains unauthenticated, and that is unchanged — anything inside the compose network can read every live session — so the row stays open on its own merits rather than on a compounding claim that has expired. The marker comes off because the reachability question was answered by running the stack; whether `requirepass` is configured is answered by reading, and the answer is no. | `requirepass` plus a credential, or network isolation | `.env.example:11`, `docker-compose.yml` |
| **SEC-10** | No TLS anywhere: inter-service traffic and the gateway itself are plain HTTP. | **DEPLOYMENT HARDENING / NON-TRAINING REQUIREMENT.** Recorded, not treated as an incident — local Compose over HTTP is ordinary and no training brief asks for TLS. It matters where a deployment exists, since the bearer token would otherwise travel in clear. | TLS termination and an internal transport policy at deployment | `docker-compose.yml` |
| **SEC-11** | Dependency audits run in CI but are **non-blocking**, and the findings have never been triaged — the step's own name says "first run, findings not yet triaged". Run 2026-08-26: frontend `npm audit --omit=dev` reports **4 (1 critical, 3 high)** — `next` 15.1.3 (dev-server information exposure; cache-poisoning DoS; fix `next@15.5.24`), `postcss` (XSS via unescaped `</style>`; file read via `sourceMappingURL`), `sharp` (inherited libvips CVEs), `nanoid` (unbounded loop). CI's `pip-audit` reports **13 findings across 2 packages** for the gateway: `cryptography` 48.0.0 (4) and `starlette` 0.41.3 (7 or more). | **OPEN ENGINEERING GAP — TRIAGE, NOT YET REMEDIATION.** A CVE in a dependency is not a demonstrated path through this application, and none of these has been traced to one. Deliberately not upgraded: a Next minor bump plus a `cryptography` bump in demo week is exactly the change that breaks a demo. `cryptography` is called out because it verifies the Ed25519 principal on the money path — the one dependency where a flaw would touch a control rather than a page. | Triage each finding against actual reachability, then a planned upgrade with the suites green; and a decision on whether CI should block | `.github/workflows/ci.yml:326`, `:340` |
| **SEC-12** | Rate limiting is a per-IP fixed-window counter (`ratelimit:{ip}:{epoch//window}`) and it **fails open**: any Redis error is caught and the request proceeds. | **TRAINING-SCOPE ACCEPTED LIMITATION / NEEDS RUNTIME VERIFICATION**, with the trade-off recorded rather than left to be discovered. Fail-open is deliberate and commented ("Redis hiccup must not block all traffic"), which is a defensible availability choice — but a Redis outage silently disables the control, and a fixed window admits a double burst at the boundary. Shared-NAT clients share a bucket. | A decision on fail-open versus fail-closed for this control, and a sliding window or token bucket if abuse is in scope | `services/gateway/app/rate_limit.py:35`, `:43` |
| **SEC-13** | When an upstream response is not JSON, `_proxy` returns its body to the caller verbatim as `{"raw": ...}`. | **FIXED 2026-09-01 (PR #158) -- AND VERIFIED BY RUNNING THE STACK, not by reading the function that changed.** *This row read `OPEN ENGINEERING GAP, narrow / NEEDS RUNTIME VERIFICATION` for a day after the fix merged, and its problem column described the passthrough in the present tense while the code no longer had one. #158 changed 112 lines of `gateway/app/main.py` and added 308 lines of tests, and touched no documentation -- which in this register is the defect being moved rather than closed.* **What replaced it.** `_proxy` no longer has an `except` that returns the body. `_json_or_refuse` parses the upstream response and, when it cannot, answers `{"detail": "upstream service returned an unreadable response"}` and nothing else. The body is never in the reply, and the log line records service, status, content-type and body LENGTH -- never the content, because a log is a place untrusted bytes would persist. **Status semantics, chosen rather than defaulted.** A body-less status (204, 304) is returned as itself; an upstream ERROR status is preserved, because a caller's retry decision depends on it and the status is not the part that leaks; an upstream SUCCESS status with an unreadable body becomes 502, because calling it 200 would assert the request succeeded while returning an error body. **What counts as parsed** is narrower than `json.loads`: every proxied route returns an object or a list -- verified route by route -- so a bare scalar such as a `text/plain` `123` is treated as unreadable rather than passed through on a technicality, and a non-finite constant is refused in the same branch as malformed JSON. **Verified at runtime**, which is what the marker on this row was promising: with the stack up, a request whose upstream answers 307 with an empty body returns `502 {"detail": "upstream service returned an unreadable response"}` -- the refusal, not the body. A repository-wide search finds `{"raw"` only inside the comment that records what the code used to do. **LIMITATION, stated because a reader could infer more.** This bounds what the GATEWAY reflects. It is not a claim that upstream services never put sensitive content in a response body -- each one's own error handling is its own question -- and it is not a general output-encoding control. What it guarantees is that an upstream body the gateway cannot parse reaches no external caller. **One behaviour change came with it and is recorded rather than left to be found:** an upstream 3xx now becomes 502, because 3xx is neither body-less nor an error status. No shipped screen depends on a proxied redirect, so nothing regressed; whether redirects should be forwarded at all is a gateway-contract question this row does not answer and does not pretend to. | Done for the gateway. The 3xx contract is a separate open question | `services/gateway/app/main.py` (`_json_or_refuse`, `_BODYLESS_STATUSES`, `_UNREADABLE_DETAIL`), `services/gateway/tests/test_upstream_body_is_never_reflected.py` |
| **SEC-14** | Retrieved policy text enters the model context with no injection screening. `contains_injection_attempt` guards the Policy Chat **question**; nothing guards the corpus content the tool returns. | **TRAINING-SCOPE ACCEPTED LIMITATION.** The corpus is committed to this repository and reviewed like code, so authorship is the control today. It becomes a real exposure if the corpus ever accepts uncommitted or third-party content. The summary path's other inputs are loaded server-side, not caller-supplied. | Corpus provenance controls, or screening of retrieved text, before the corpus accepts outside content | `services/loan-assistant/app/policy_chat.py:148`, `services/loan-assistant/app/policy_tool.py` |
| **SEC-15** | `logs/payment-service.log` was untracked in `32fdd04` but remains reachable in history (also `a726640`), carrying card-shaped and SSN-shaped values. Verified without printing them: the only distinct 16-digit PAN is the published Visa **test** number, and the 14 SSN-shaped values carry the same prefixes as `db/init/002_seed.sql`, so they are seed-derived. | **HISTORICAL SYNTHETIC ARTIFACT / PROVENANCE GAP.** Not a cardholder-data breach and it must not be described as one. The real defect is provenance: the committed seed uses realistic SSN prefixes while `frontend/e2e/fixtures.ts` uses the unmistakably fictional `999-` range, so synthetic values are indistinguishable from real ones to anyone auditing. See D18 for the log file itself. | A history rewrite needs repository-owner coordination and a force-push, and is **not** done here | history of `logs/payment-service.log`; `db/init/002_seed.sql`; `frontend/e2e/fixtures.ts:81` |
| **SEC-16** | Downstream services take the forwarded `X-User-Role` as an authorization input. `loan-assistant`'s `POST /applications/{id}/summary` took it as the *only* one: it declared no `X-Internal-Token` parameter, the app had no global dependency, and it then called origination-service's staff-only `/financials` with its **own** internal token and the caller's claimed role -- a confused deputy for staff-only application data. | **VERIFIED 2026-08-27, THEN FIXED. RESIDUAL AT SEC-17.** *This row read `BOUNDED TRUST BOUNDARY -- NOT A GENERAL HOLE / NEEDS RUNTIME VERIFICATION` until 2026-08-27, and both halves expired on the same day: the runtime verification happened, and it found the boundary wider than "bounded" implied -- what held it shut was Compose topology, not a check.* **Reproduced** against the running stack from inside the Compose network, no host port opened: `POST loan-assistant:8007/applications/4471/summary` carrying only `X-User-Role: underwriter` returned **200** with the applicant's name, loan amount and financials, while origination refused the identical role-only call for `/financials` with **403**. The same run confirmed the outer boundary holds -- through the gateway a borrower got 403, a borrower sending `X-User-Role: admin` got 403, an anonymous caller sending it got 401 -- which is why this was a gap and not an incident. **Fixed** by the pattern already used elsewhere: `_require_staff` requires a valid `X-Internal-Token` **and** a staff role, compared with `secrets.compare_digest`, failing closed on an unset configured token, running before the trace opens so a refused request mints no run; the gateway forwards that token on `/assistant/*` as it already did on `/los/*`. Re-verified after the fix: role-only direct **200 -> 403**, no-headers **500 -> 403**, staff via gateway still 200. A smaller defect closed with it: a caller with no identity used to get 500, because origination refused the token-without-role fetch and `raise_for_status()` turned that into an unhandled error. Closed for this control; the symmetric-token residual is tracked at **SEC-17** rather than left inside a row marked fixed | None for this control -- it is closed. See SEC-17 for what remains | `services/loan-assistant/app/main.py:62`, `services/gateway/app/main.py:631`; `services/loan-assistant/tests/test_summary_requires_the_gateway_token.py`, `services/gateway/tests/test_assistant_carries_the_internal_token.py` |
| **SEC-17** | The internal service token is **symmetric**: every backend holds the same value, so a service that holds it can set `X-User-Role` to anything and be believed. SEC-16's fix raises `loan-assistant` to the same standard as origination, payment, kyc and decision -- it does not raise that standard. `principal.py` says so directly, and `specs/0002-maker-checker-self-approval.md` REQ-ID-3 is the requirement written against it. | **OPEN / BOUNDED, AND DELIBERATELY NOT CLOSED HERE.** Split out of SEC-16 on 2026-08-27 so that closing one control did not quietly absorb the limitation underneath it. The answer that would close it already exists on the money path: servicing verifies a gateway-signed Ed25519 principal and treats the headers as untrusted hints. Extending that to staff-gated **read** routes is a design change with its own blast radius -- an assertion minted per proxied request on every `/assistant/*` and `/los/*` hop -- and is not something to attach to a route fix. Reachable only from inside the Compose network, as SEC-16 was. | A decision on whether the principal assertion extends past the money path, then the work to do it | `services/gateway/app/principal.py:5`, `specs/0002-maker-checker-self-approval.md` REQ-ID-3, `services/servicing-service/app/main.py:14` |

**Checked and found not to be a defect.** `services/gateway/app/main.py` calls
`Instrumentator().instrument(app).expose(app)` twice, at lines 74 and 80. It was
reproduced rather than judged by eye: two `/metrics` routes are registered, but
the metrics are **not** double-counted — three requests to `/health` read
`http_requests_total ... 3.0` and `http_request_duration_seconds_count ... 3.0`,
`/metrics` returns 200, and no warning is emitted at import. FastAPI matches the
first route, so the second is dead. Redundant, harmless, left alone.

**What NEEDS RUNTIME VERIFICATION means here.** This pass was static plus
in-process, and the marker is on the rows where that is load-bearing rather than
across the section as a whole — four of the seventeen: **SEC-02** (logout
invalidating server-side across a real session), **SEC-03** (cross-origin
behaviour in a browser), **SEC-05** (response headers as actually served, not as
configured) and **SEC-12** (fail-open under an actual Redis outage rather than
by reading the `except`).

The other thirteen are not marked, for two different reasons. Eight are settled by
reading the repository, and running it a second time would not change the answer: `sha256`
password hashing (**SEC-01**), the `localStorage` token and the absence of any
injection sink (**SEC-04**), the missing `USER` directive in all nine Dockerfiles
(**SEC-06**), the published Postgres port the host-side suites already exercise
(**SEC-07**), plain HTTP throughout (**SEC-10**), the audit counts (**SEC-11**),
the unscreened corpus path (**SEC-14**) and the log file's history (**SEC-15**).

The remaining five are unmarked for the opposite reason, and the first of them is
the point of having the marker at all. **SEC-08** and **SEC-09** are the newest
instances: both carried the marker because the question was reachability from the
host, and that question was answered on 2026-08-31 by bringing the stack up —
`docker compose ps`, a socket probe and an in-network `PING` — rather than by
reading the compose file that changed. SEC-08 is fixed; SEC-09 stays open on the
half a run cannot settle, which is that no password is configured. **SEC-16** carried it because its
conclusion depended on running the stack; the stack was run on 2026-08-27, the
boundary turned out to be wider than the row had claimed, and the control is now
implemented and re-verified — so the row states a measured result instead of a
pending one. A marker that never came off would be decoration. **SEC-17** is a
property of the design — one shared secret, held by every backend — and no run
could disprove it, so a marker promising future verification would be promising
something that will never arrive. **SEC-13** carried the marker because the
question was what a real non-JSON upstream body contains; the stack was brought
up on 2026-09-01, an upstream 307 with an empty body came back as the refusal
rather than the body, and the row now states a measured result. A marker that
never came off would be decoration.
Marking a statically-proven fact as runtime-unknown would be as misleading as the
reverse. `db/tests/test_security_register_marks_what_it_cannot_prove.py` holds
this footer and the table to each other.

## Not in this register

Two things are deliberately absent, so their absence is not mistaken for
coverage:

- **No severity ranking.** Assigning one would need a data-flow audit
  (which fields, reaching which sinks, readable by whom) that has not been
  done. An unranked list is more honest than an invented ordering.
- **No target dates.** This is a local training build with no production
  deployment and no on-call, so a due date here would be decoration.
