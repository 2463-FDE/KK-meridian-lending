fastapi>=0.115.0
uvicorn[standard]>=0.34.0
# Upper bound, not a preference. `anthropic` 1.0.0 was released mid-week and CI
# resolves at install time, so the same commit passed at 19:54 and failed at
# 20:10 on 2026-08-20 -- 0.125.0 then 1.0.0, nothing else changed. The visible
# break was AnthropicBedrock() raising when no AWS region can be inferred
# instead of tolerating it; a major bump is not a promise that this is the only
# behaviour change, and the rest have not been evaluated.
#
# Held below 1.0.0 until someone reads its changelog against llm_client.py and
# moves the bound deliberately. A floor with no ceiling on a money-adjacent
# service means an unrelated PR can go red for a reason nobody chose.
anthropic>=0.40.0,<1.0.0
httpx>=0.27.0
tenacity>=8.2.0
pydantic>=2.0.0
langsmith>=0.10.0
prometheus-fastapi-instrumentator==7.0.0

# --- agentic underwriting summary (LangChain v1, Bedrock) ---------------------
#
# EXACT pins, not floors. This is freeze-bound work and CI resolves at install
# time -- the same drift that turned an unrelated PR red on 2026-08-20 (see the
# anthropic bound above) would be worse here, because it would change the agent
# runtime under a demonstration.
#
# Scoped to THIS service. decision-service and origination-service pin
# langgraph==0.2.62 for their own graphs and are untouched: each service has its
# own requirements file, its own Dockerfile and its own CI job, so nothing here
# reaches them. Verified by dependency-resolution dry-run before adding.
#
# `anthropic` stays below 1.0.0 and is NOT pulled by these: langchain-aws talks
# to Bedrock through boto3. The existing boundary is preserved deliberately.
langchain==1.3.16
langchain-aws==1.7.3
langchain-core==1.6.0
langgraph==1.2.11

# WHAT THIS DOES AND DOES NOT LOCK, stated because the PR claimed more than it
# enforced (reviewed on PR #63, finding 6).
#
# Locked: the four direct agent dependencies above, exactly.
#
# NOT locked: their transitive closure. A clean checkout today resolves
# langgraph-checkpoint 4.2.0, langgraph-prebuilt 1.1.0, langgraph-sdk 0.4.3,
# langchain-protocol 0.0.18, boto3/botocore 1.43.77 and numpy 2.5.2 -- a future
# install may resolve different versions of any of them within the ranges the
# pinned packages allow.
#
# Not pinned here on purpose: this repository's convention across all eight
# services is exact pins on DIRECT dependencies only, with no constraints file
# anywhere, and pinning a transitive graph by hand is how a service acquires a
# combination its own maintainers never tested. If the demo needs a fully
# reproducible closure, the right tool is a lock/constraints file adopted
# repo-wide, not a partial list bolted onto one service.
